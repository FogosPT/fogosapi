get:
  operationId: getNewFires,
  summary: List active fires
  tags:
    - Fires
  responses:
    200:
      $ref: '../responses/legacy-fires-list.yaml'
    500:
      $ref: '../../common/responses/500.yaml'
