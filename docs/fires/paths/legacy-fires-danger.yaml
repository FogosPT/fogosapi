get:
  operationId: getFiresDanger,
  summary: Get fire RCM for today, tomorrow and after
  tags:
    - Fires
  parameters:
    - $ref: '../parameters/query/id.yaml'
  responses:
    200:
      $ref: '../responses/legacy-fires-danger-list.yaml'
    404:
      $ref: '../../common/responses/404.yaml'
    500:
      $ref: '../../common/responses/500.yaml'
