get:
  operationId: getMadeiraFires
  summary: List fire incidents for Madeira (deprecated, returns empty)
  tags:
    - Fires
  responses:
    200:
      $ref: '../responses/legacy-fires-list.yaml'
    500:
      $ref: '../../common/responses/500.yaml'
